version: '2'
services:
  postgres:
    image: postgres:13.1
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test
    #volumes:
    #  - ./init.sql:/docker-entrypoint-initdb.d
